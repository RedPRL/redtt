(jbuild_version 1)

(executables
 ((names (main))
  (ocamlopt_flags (:standard -p))
  (libraries (cmdliner lwt.unix redtt))))

(install
 ((section bin)
  (package redtt)
  (files ((main.exe as redtt)))))
